import 'package:flutter/material.dart';

class CollabTeamScreen extends StatelessWidget {
  const CollabTeamScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('👥 협업 모드 (팀 프로젝트)', style: TextStyle(fontSize: 18)),
    );
  }
}