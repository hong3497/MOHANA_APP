import 'package:flutter/material.dart';

class CollabPersonalScreen extends StatelessWidget {
  const CollabPersonalScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('🙋 개인 모드 (나의 프로젝트)', style: TextStyle(fontSize: 18)),
    );
  }
}